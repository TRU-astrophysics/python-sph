primarily using var_smooth_length_sim

inputs are:
 - time_arr: ndarray(Nt)
 - positions0: ndarray(N,3)
 - velocities0: ndarray(N,3)
 - energies0: ndarray(N)
 smoothlength_approx: ndarray(N)

outputs:
 - positions: ndarray(Nt, N, 3)
 - velocities: ndarray(Nt, N, 3)
 - energies: ndarray(Nt, N)
 - smoothlengths_with_time : ndarray(Nt, N)
 
 Need to run simulation multiple times, then calculate the total change in energy over time for each simulation
 I will store the energy changes into an array that will then be used to plot each total energy onto the same plot
 
 I might be able to get around the use of a total energy array and just plot between runs and then show the plot after the algorithm has completed each