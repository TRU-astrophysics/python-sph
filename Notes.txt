primarily using var_smooth_length_sim

inputs are:
 - time_arr: ndarray(Nt)
 - positions0: ndarray(N,3)
 - velocities0: ndarray(N,3)
 - energies0: ndarray(N)
 smoothlength_approx: ndarray(N)

outputs:
 - positions: ndarray(Nt, N, 3)
 - velocities: ndarray(Nt, N, 3)
 - energies: ndarray(Nt, N)
 - smoothlengths_with_time : ndarray(Nt, N)
 
Total energy decreases over time
Internal energy decreases significantly over time, error must be there
I think energy rate is removing some energy. So I will add a line of code that allows for the output of energy removed from the system due to viscosity.
Then I will add this to the total energy function

I need to test if the total_energy function is equivalent to the individual energies being calculated in the loop